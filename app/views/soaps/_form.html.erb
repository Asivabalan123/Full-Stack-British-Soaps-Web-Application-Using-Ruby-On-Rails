<%= form_with(model: soap, local: true) do |form| %>
  <% if soap.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(soap.errors.count, "error") %> prohibited this soap from being saved:</h2>

      <ul>
      <% soap.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
